// Package example is the placeholder domain client for the mcp-template.
// Replace the package (and its imports) with your real upstream system —
// Prometheus, Grafana, Opsgenie, etc. The interface + fake pattern is what
// matters: tools depend on the interface, tests use the fake.
//
// scripts/init.sh rewrites internal/example -> internal/<your-domain>.
package example
