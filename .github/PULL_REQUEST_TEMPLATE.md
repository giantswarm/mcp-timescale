## Summary



### Checklist

- [ ] Updated `CHANGELOG.md` (`### Added` / `### Changed` / `### Fixed` / `### Removed` under `## [Unreleased]`).
- [ ] `helm lint` passes; `values.yaml` and `values.schema.json` are valid.
- [ ] Tests added or updated.
